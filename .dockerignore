# Python
**/__pycache__
**/.venv

# Editor / VCS
**/.git
**/.gitignore
**/.dockerignore
**/.vscode
**/.env

# Dados e checkpoints baixados/gerados em runtime — nunca fazem parte da
# imagem, e sem isso o build context arrasta o CIFAR-10 já baixado (~187 MB)
# a cada `docker compose up --build`.
data/
*.pth

# A própria imagem não precisa destes arquivos dentro dela
**/docker-compose*
**/compose*
**/Dockerfile*

# Docs
README.md
LICENSE
